// The Play sbt plugin wires up the PlayScala enablePlugin, hot-reload dev server,
// routes compiler, and asset pipeline. Without it, Play is just a library with no build magic.
addSbtPlugin("com.typesafe.play" % "sbt-plugin" % "2.9.10")
