version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: parcel_tracker
      POSTGRES_USER: parcel_user
      POSTGRES_PASSWORD: parcel_pass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
